# Versions pinned to those the app is developed and verified against locally.
# Unpinned deploys resolved different major versions (notably pandas 2.x vs
# 3.x), which changed query-result handling and produced silently-empty
# tables rather than errors.
streamlit==1.58.0
pandas==3.0.3
numpy==2.5.0
plotly==6.8.0
snowflake-snowpark-python==1.52.0
snowflake-connector-python==4.6.0
cryptography==49.0.0
python-pptx==1.0.2
